# PR #288 审查 5286056434 处理记录

审查：[5286056434](https://github.com/hailingu/PlotWeave/pull/288#pullrequestreview-5286056434)，基准 `d775389b443b5b523831e8d3f310ed0489dfdc33`。这是该提交唯一的一轮跟进，共两项 P2：

- 第一项有新触发条件：合法查询 `(prefers-reduced-motion: no-preference)` 在所有建模环境里都不生效，属于真实漏检。
- 第二项所述缺陷不成立，属于误报：不带空白的子代组合器早已被识别。

之后再有非 P1 发现，按轮次预算关闭，交由仓库所有者裁决。

## 关键状态与不变量矩阵

| 前提/状态 | 动作/过渡 | 可观察结果 | 不变量与所有者/入口 | 验证 |
| --- | --- | --- | --- | --- |
| 样式表写 `@media (prefers-reduced-motion: no-preference)`，块内消费类型不相容的令牌 | 在基线动效环境做类型扫描 | 点名违例；在 `reduce` 环境不点名 | 环境值使用 CSS 关键字；基线动效为 `no-preference`。所有者为 `Env`/`LIGHT_ENV`、`featureMatches`，入口为 `conditionActive` 及全部扫描 | 语义夹具正反两例 |
| 媒体取值不在建模域内（`no-reduce`、合法但未建模的 `prefers-contrast: less`、非法的 `sepia`） | 条件求值 | 抛出 `TOKEN_MEDIA_VALUE_UNMODELED` | 域外取值必须明确失败，不能静默失活 | `it.each` 3 例 |
| 真实样式表里的 `reduce`/`more`/`dark` 查询 | 8 个配对环境与接线/类型全表扫描 | 行为不变 | 已建模的取值照常求值 | 全表契约测试 |
| 祖先定义局部遮蔽，子代写成 `.parent>.child` 或 `.parent > .child` | 类型扫描 | 两种写法都点名 `color: 4px` | `selectorReaches` 中的 `\s*>` 本来就接受零个空白 | 实际解析夹具 2 例，以及 `scopeReaches` 用例 |

范围说明：

- 建模域为 `scheme: light|dark`、`contrast: no-preference|more`、`transparency: no-preference|reduce`、`motion: no-preference|reduce`。
- `prefers-contrast: less/custom` 是合法 CSS，但目前未建模，因此明确拒绝，不按失活处理。
- 没有运行时并发、重试或持久化方面的变化，这些维度不适用。

已知旁路：[nodeTokens.test.ts](../../src/editor/nodes/nodeTokens.test.ts)、[panelsTokens.test.ts](../../src/editor/panels/panelsTokens.test.ts)、[controlsTheme.test.ts](../../src/editor/nodes/controlsTheme.test.ts) 各自复制了一份媒体模型，同样以 `no-reduce` 为基线。

- 这些文件不在本 PR 的改动范围内，属于既有问题。
- 它们扫描的样式表目前只用 `reduce`，所以没有实际漏检。
- 本轮没有修改，建议另开 issue 跟踪。

## 验证记录

工作目录为仓库根目录，Node 为 `.nvmrc` 固定的 `24.18.0`。

- 探针：临时文件 `src/styles/review-5286056434-probe.test.ts`，已删除。
  - `no-preference` 块的类型扫描返回 `[]`，确认第一项属实。
  - `.parent>.child` 与 `.parent > .child` 都返回 `color: 4px`，确认第二项为误报。
- Red：运行 `npx vitest run src/styles/sheetTokensSemantics.test.ts`，结果为 4 失败、148 通过。
  - 失败项是 `no-preference` 漏检和 3 个域外取值未拒绝。
  - 两个子代组合器用例直接通过。
- Green：`npx vitest run src/styles` 共 252/252 通过。
- 完整路由：`npm run format:check && npm run lint && npm run typecheck:strict && npm run build && npm test` 全部通过，共 151 个文件、2094 项。
  - `git diff --check` 通过，构建产生的 `dist/` 已删除。
  - 文件行数：引擎 736、语义测试 981，均低于上限。
  - 新增的 `featureMatches` 低于 20 行。
- 未改 Rust 源码，Rust 路由不适用。提交 `8d53a7f` 的提交与推送门禁均通过：Quality Gate 为 OK，新代码未解决问题 0。

## 原线程处置依据

### [基线动效 no-preference](https://github.com/hailingu/PlotWeave/pull/288#discussion_r4078349310)

已修复，真实 P2 漏检。

- `Env.motion` 的基线改为 CSS 关键字 `no-preference`。
- 新增 `MEDIA_VALUES` 建模域：`featureMatches` 对域外取值抛出 `TOKEN_MEDIA_VALUE_UNMODELED`，关掉「合法关键字静默失活」这一类问题。

### [无空白子代组合器](https://github.com/hailingu/PlotWeave/pull/288#discussion_r4078349314)

不改代码，属于 P2 误报。

- `selectorReaches` 的 `/^(\s*>|…)/` 中 `\s*` 允许零个空白，`inheritanceDistance` 按 `/[\s>]+/` 切分。
- 审查所举的原例会被点名为 `.parent>.child color: 4px`。
- 已补回归夹具锁定该行为。
