# PR #288 审查 5277799858 处理记录

审查：[5277799858](https://github.com/hailingu/PlotWeave/pull/288#pullrequestreview-5277799858)，基准提交 `1978ea65627c37e206bf8c026969ae1ba361a822`。范围为当前分支的修复、验证、提交推送及原线程回复。

三条意见均提供当前提交的新证据，属于该提交的一轮跟进审查。文件行数超限为真实仓库规则违例，必须修正；另两条为 P2 契约校验漏检。没有已证实的 P1 用户数据损失触发条件。

接手时暂存区已有同范围的拆分和修复：`sheetTokens.test.ts`、`sheetTokensEngine.ts`、`sheetTokensSemantics.test.ts`。保留这些改动，复核共享引擎、补充验证证据及设计说明；无关的 `2026-09-22-quality-assessment.md` 不纳入提交。既有修复的编写顺序不可追溯，本记录不宣称它是在本轮新测试后才编写；使用审查提交的临时副本复现旧行为，并在当前实现验证相同用例。

## 关键状态与不变量矩阵

| 前提/状态 | 动作/过渡 | 可观察结果 | 不变量与所有者/入口 | 验证 |
| --- | --- | --- | --- | --- |
| 审查版本测试文件 1802 物理行 | 拆为真实样式表契约、共享引擎、语义夹具 | 各文件符合对应行数上限，原测试语义保留 | `sheetTokensEngine.ts` 是两套测试的共同解析入口，拆分不复制引擎 | 人工行数/函数跨度检查，原套件及完整路由 |
| `background-color` 使用字面色、尺寸令牌或局部字面别名 | 归一属性名并进入结构/类型扫描 | 字面硬编码和不相容值被点名，有效语义令牌通过 | `isDisplayColorProp` 同时供字面消费闭包、全表结构及 `displayTypeErrors` 使用，长形背景色不得遗漏 | 大小写与有效/无效消费语义用例，真实新增 CSS 扫描探针 |
| 根令牌同名普通/重要声明按不同顺序出现 | 取值后经 `color` 消费 | 重要声明优先，同重要性取后位；无效颜色不被普通值掩盖 | `tokenValuesOf` 为 `tokenValues` 及消费扫描提供根值，重要性先于源序 | 普通/重要性顺序组合及消费结果用例 |
| 根令牌跨基线、媒体条件和后续基线声明 | 按环境筛选后选择根值 | 仅活跃声明参与，活跃重要值不被普通值覆盖 | 所有根令牌入口使用同一选胜规则，非活跃环境不改变结果 | 浅/深条件正反例及同重要性源序用例 |
| 重要根值为 initial 或断链，消费点有 fallback | 根值失效后取回退 | 有效回退恢复，无效回退报错 | 重要性选胜不能绕过既有无效值/fallback 类型规则 | 根值到消费结果的失败/恢复用例 |

无应用生命周期、并发、重试或持久化变更，这些维度不适用。完整 CSS 文法、颜色函数内部参数、复杂选择器和跨选择器特异性保留既有边界；不运行 WebView 像素测试，因为本轮改变静态契约引擎。

## 验证结果

工作目录为仓库根目录，使用 `.nvmrc` 固定的 Node `24.18.0`。基准复现位于该审查提交的临时归档副本，未回退或覆盖现有工作区。

- 旧行为复现：把新增 17 个语义用例放到审查提交的临时副本中，`npm test -- src/styles/sheetTokens.test.ts` 得到 9 个预期失败、90 个通过。失败为背景长形漏分类、重要根值被后位普通值覆盖及其媒体/回退场景。最初夹具遗漏末尾分号导致的 2 个空白字符串断言失败已修正，未计入上述缺陷复现证据。
- 当前行为：`npm test -- src/styles/sheetTokensSemantics.test.ts src/styles/sheetTokens.test.ts`，100/100 通过。包含暂存改动已有的 1 个重要性用例和本轮补充的 17 个消费用例，原语义测试保留。
- 真实扫描探针：临时 CSS 含 `background-color: #fff`、`BACKGROUND-Color: var(--fill)`（局部 `--fill: #fff`）和 `background-color: var(--radius-sm)`。运行 `npm test -- src/styles/sheetTokens.test.ts -t '展示色结构：全表扫描与注册表'`，基准版本 4 项全部误通过；当前版本结构和类型两个断言按预期失败，分别点名字面声明/局部别名和全部 16 个环境的 `4px` 消费。探针随后删除，正常套件全绿。
- 完整路由：`npm run format:check && npm run lint && npm run typecheck:strict && npm run build && npm test` 全部通过；149 个文件、1942 个测试通过。`git diff --check` 通过。
- 上限检查：`wc -l` 为真实契约测试 836 行、共享引擎 552 行、语义测试 643 行。用 TypeScript AST 定位可执行单元并剔除纯注释行，最长分别 66、32、66 代码行，无超过 80 行的函数或回调。拆分时发现迁移来的作用域测试分组回调有 99 行，按作用域可达与局部级联两个职责分组，保留全部用例。
- 分解复核：真实 CSS 扫描与合成夹具共用一个取值引擎；只在引擎内部使用的 7 个函数不再导出。66 行测试分组跨过讨论阈值，但均集中于同一契约的平行语义案例，没有新嵌套状态或复杂控制流；另拆通用辅助层会增加阅读跳转。圈复杂度为 `N/A — no configured complexity tool`，以函数跨度和控制流复核代替；引擎控制流未新增超过 4 层的嵌套。
- 首次提交门禁发现 `typescript:S3776`：根令牌遍历的认知复杂度 17 超过上限 15。抽取 `applyRootRule`，分离活跃规则遍历与直接根声明合并，保留源序和取胜语义；聚焦 100/100 再次通过，再运行完整门禁，未抑制规则。
- `docs/**` 无配置自动行为检查；已结构化核对设计 §2.1、两套测试头部矩阵、本文、审查触发条件和保留边界。无后端变更，Rust 变更路由不适用。提交与推送的版本化钩子分别刷新前端/Rust 覆盖率并运行 SonarQube，最终提交与门禁结果记录在原线程。
- 临时归档、CSS 探针、构建及门禁输出在完成后清理；任务开始前已有产物恢复，无关文档保持原样。

## 原线程处置依据

### [测试文件超限](https://github.com/hailingu/PlotWeave/pull/288#discussion_r4071422237)

1. **处置** — 已修复，真实仓库规则违例。审查提交的文件确有 1802 物理行，不能按新文件豁免；没有 P1 用户数据损失触发证据。
2. **理由与变更** — 按职责拆为真实样式表契约、CSS 取值引擎和语义夹具；两套测试共享同一实现，并控制导出接口。进一步拆开 99 行分组回调，未删除测试来降低行数。
3. **验证** — 文件/可执行单元检查结果见上；聚焦 100 项与完整 1942 项测试通过。
4. **后续** — 本条无剩余工作。

### [background-color 漏分类](https://github.com/hailingu/PlotWeave/pull/288#discussion_r4071422241)

1. **处置** — 已修复，真实缺陷，P2。
2. **理由与变更** — `DISPLAY_PROPS` 纳入 `background-color`，沿用现有属性名归一规则；同一分类器供字面扫描、局部变量消费闭包和类型扫描使用。
3. **验证** — 大小写长形的尺寸值被拒绝、语义色和 currentcolor 通过；真实 CSS 探针确认字面值、局部别名及不相容令牌均能由全表入口报告。基准复现与当前结果见上。
4. **后续** — 本条无剩余工作；静态 CSS 文法边界不变。

### [根令牌重要性](https://github.com/hailingu/PlotWeave/pull/288#discussion_r4071422254)

1. **处置** — 已修复，真实缺陷，P2。
2. **理由与变更** — `tokenValuesOf` 仅在声明胜出时更新根值：重要性优先，同重要性按真实源序后写覆盖；媒体环境先筛选。重要值失效仍经既有消费解析进入 fallback。
3. **验证** — 根值经 `displayValueIn` 到 `colorTypeOk` 的消费结果覆盖声明顺序、同重要性后写、浅/深媒体条件、initial/断链以及有效/无效回退；聚焦与完整路由通过。
4. **后续** — 本条无剩余工作；跨选择器特异性和完整 CSS 层叠引擎仍为已记录边界。
