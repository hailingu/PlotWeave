# PR #288 审查 5280837519 处理记录

审查：[5280837519](https://github.com/hailingu/PlotWeave/pull/288#pullrequestreview-5280837519)，基准 `9df32c97a1d3cdc9f2e58656001a9365126ee78c`。四项均为新 P2 契约漏检（新入口或新触发条件），属于该提交唯一的一轮跟进；按上一轮处置流程修复、验证、提交推送并在原线程回复。之后再有非 P1 发现按轮次预算关闭，交由仓库所有者裁决。

## 关键状态与不变量矩阵

| 前提/状态 | 动作/过渡 | 可观察结果 | 不变量与所有者/入口 | 验证 |
| --- | --- | --- | --- | --- |
| 组件表在 `text-emphasis-color`、`scrollbar-color`、厂商前缀 `-color` 长形或 `text-emphasis` 简写上写字面色或消费尺寸令牌 | 结构/类型扫描 | 字面色按未审计违例点名，尺寸令牌按类型不相容点名 | 展示色分类按结构（`color` + 标准 `-color` 长形），不靠枚举；所有者 `isDisplayColorProp`，结构、类型、闭包三处入口共用 | 分类正例 5 项、类型夹具、真实 glob 探针 |
| 自定义属性名以 `-color` 结尾（如 React Flow `--xy-…-color`）、`color-scheme`、`text-emphasis-style` | 分类 | 不进入展示色契约 | 结构分类不误伤自定义属性和非颜色属性 | 分类反例 3 项；真实表全部通过 |
| 合法强调标记关键字（`filled`/`open`/`sesame`/`double-circle`）和双色 `scrollbar-color` | 类型扫描 | 通过 | 关键字形态与多值颜色列表不误报 | 类型夹具正例 |
| 组件表或无来源夹具含 `@property`（大小写不敏感、可嵌在 media 中），即使无消费者 | 声明枚举/接线/类型入口 | 报 `TOKEN_GLOBAL_OUTSIDE_SOURCE` | 注册总是全局生效，只能来自令牌源；由 `assertGlobalTokenSource` 统一预检 | 2 组 × 3 布局 × 3 入口；真实 glob 探针 |
| 令牌源 `tokens.css` 含 `@property` | `tokenValuesOf` 与 `sheetDecls` | 报 `TOKEN_ROOT_AT_RULE_UNMODELED` | 注册会改变根值的合法性，尚未建模，必须显式失败而不是静默取值 | 取值与枚举入口各 1 项 |
| 展示消费点与同名局部定义的选择器互不可达（后者只给 `box-shadow` 用） | 结构扫描的消费闭包 | 不可达定义不入契约；可达定义（含 media 条件、间接值链）仍入契约 | 闭包按（消费分支, 定义）的可达对计算，与接线/类型扫描同为 `scopeReaches` | 可达性夹具（2 个不可达排除 + 3 个可达保留）+ 既有闭包用例 |
| 危险规则中 `background: var(--danger)` 之后有 `background-color`/`background-image`，或更早的长形带 `!important` | 黄金接线 | 胜出声明不是 `background: var(--danger)` 即判违例；简写重置更早的普通长形时仍通过 | 危险底色按实际胜出声明判定；所有者 `winningDecl`，`declOf` 复用同一取胜规则 | 夹具 4 项 + 真实四处规则 |

范围说明：强调标记字符串（如 `text-emphasis: "x"`）和完整简写文法仍未建模，遇到时会明确报错，不会被静默放行。闭包对任一环境条件都保守纳入可达定义，不按环境逐一求胜出值。跨选择器特异性与复杂选择器仍是既有边界。危险前景只核对 `color` 一个属性，`-webkit-text-fill-color` 等其他覆盖前景的入口不在本轮范围内。没有运行时并发、重试或持久化变化，相关维度不适用；未运行 WebView 像素测试。

## 验证记录

工作目录为仓库根目录，Node 为 `.nvmrc` 固定的 `24.18.0`。

- Red：新增/改写回归后，`npx vitest run src/styles/sheetTokenBoundaries.test.ts src/styles/sheetTokens.test.ts` 得到 12 个预期失败、47 个通过。失败项：`@property` 3 项未抛错；分类 5 项和类型 1 项未识别；闭包纳入不可达定义（实得 5 项，应为 3 项）；危险接线 2 项缺少 `winningDecl`。闭包先按原语义改为以定义身份计数（等价重构），再观察可达性断言失败。
- Green：`npx vitest run src/styles` 共 232/232 通过。可达性夹具首跑失败，原因是夹具末尾声明缺分号，PostCSS 保留了自定义属性值的尾随空白；补上分号后通过，不涉及实现。
- 真实 glob 探针：临时新增 `src/styles/.review-5280837519-probe.css`。内容为 `@property --text-primary {…}` 时，`npx vitest run src/styles/sheetTokens.test.ts` 得到 6 个预期失败，均为 `TOKEN_GLOBAL_OUTSIDE_SOURCE`；内容为 `.probe { text-emphasis-color: #fff; }` 时，结构扫描把它点名为未审计字面色。探针已删除。
- 完整路由 `npm run format:check && npm run lint && npm run typecheck:strict && npm run build && npm test` 全部通过：151 个文件、2074 项。`git diff --check` 通过。本次构建新建的 `dist/` 已删除；无关未跟踪文档 `2026-09-22-quality-assessment.md` 未改动。
- 文件行数（`wc -l`）：引擎 697、`cssColorContract.ts` 182、真实契约测试 940、边界测试 240，均低于上限。新增或改动函数最长的是 `displayConsumedDefs`（约 30 行），低于 80 行。圈复杂度：未配置复杂度工具，结果为 `N/A — no configured complexity tool`。
- 文档路由没有配置自动检查，已结构化核对 `docs/ui-design.md` §2.1、测试头注矩阵及错误码。未改 Rust 源码，Rust 路由不适用；提交与推送门禁照常刷新覆盖率并运行 SonarQube，最终结果和提交引用发布在原线程。

## 原线程处置依据

### [text-emphasis-color 分类](https://github.com/hailingu/PlotWeave/pull/288#discussion_r4073956493)

已修复，真实 P2 漏检。展示色分类改为结构式：`color` 加全部标准 `-color` 长形（含厂商前缀），自定义属性除外；`text-emphasis` 简写和强调标记关键字也纳入，`scrollbar-color` 按双色列表校验。

### [@property 全局注册](https://github.com/hailingu/PlotWeave/pull/288#discussion_r4073956505)

已修复，真实 P2 漏检，采用拒绝方案。组件表和无来源夹具中的 `@property` 报 `TOKEN_GLOBAL_OUTSIDE_SOURCE`，令牌源内的注册报 `TOKEN_ROOT_AT_RULE_UNMODELED`，不实现注册语义。

### [消费闭包按可达作用域](https://github.com/hailingu/PlotWeave/pull/288#discussion_r4073956520)

已修复，真实 P2 误报。闭包改为按（消费分支, 定义）的可达对传递，结构扫描按定义身份匹配，不可达的同名局部复用不再被点名。

### [危险底色背景长形](https://github.com/hailingu/PlotWeave/pull/288#discussion_r4073956527)

已修复，真实 P2 漏检。新增引擎函数 `winningDecl`，把 `background` 与 `background-color`/`background-image` 同组按重要性和源序取胜，胜出项必须是 `background: var(--danger)`；`declOf` 复用同一取胜规则。

> 后续修正：单组取胜会误判 `background: var(--danger); background-image: none` 这类合法规则，[审查 5285927947](pr-288-review-5285927947.md) 已改为颜色与图像成分独立层叠。
