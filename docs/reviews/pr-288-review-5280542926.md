# PR #288 审查 5280542926 处理记录

审查：[5280542926](https://github.com/hailingu/PlotWeave/pull/288#pullrequestreview-5280542926)，基准 `6bf4c7849068f26eb1402bafb6d58fd6ead50a8a`。两项均为新 P2 契约漏检，属于该提交的一轮跟进；在既有修复、验证、提交推送及原线程回复授权内处理。

## 关键状态与不变量矩阵

| 前提/状态 | 动作/过渡 | 可观察结果 | 不变量与所有者/入口 | 验证 |
| --- | --- | --- | --- | --- |
| 组件表定义全局自定义属性，本表无消费者而其他表有消费者 | glob 发现后解析带来源的声明 | 定义表报 `TOKEN_GLOBAL_OUTSIDE_SOURCE`，不因导入顺序或本表消费者缺失而放过 | `tokens.css` 是全局令牌唯一源；真实表枚举与接线/类型共用 `sheetDecls` 入口 | root/html/body/通配、逗号分支、媒体及真实 glob 探针 |
| token 源中全局定义，或组件表仅有局部定义/普通全局属性 | 解析声明与消费 | 合法输入通过，保留单表局部遮蔽 | 所有权限制只针对组件全局自定义属性，不禁止全局普通样式 | 定义源、局部/普通属性正例 |
| 自定义属性胜出值为非根 unset、inherit、revert 或 revert-layer | 接线与类型解析消费点 | 明确报 `TOKEN_CSS_WIDE_UNMODELED` | 不把自定义属性的继承/回滚语义透传成消费属性关键字；`isGuaranteedInvalid` 由根/局部作用域取值共用 | 根/局部、祖先、别名链、媒体及重要性选胜入口 |
| initial 或文档根 unset，消费点有有效/无效 fallback | 解析保证无效值并回退 | 按既有规则恢复或拒绝，真实悬空仍报告 | 已建模保证无效值不受拒绝边界破坏 | root/html unset、initial、fallback 正反例 |
| 普通 color 声明使用 CSS-wide keyword 或自定义值只是字符串 | 类型检查或内容取值 | 既有合法语法保留 | 拒绝仅适用于自定义属性的未建模取值，不误伤消费属性关键字或字面字符串 | 对照正例 |

本轮选择明确拒绝，不实现跨文件完整级联或 CSS-wide 自定义属性继承/回滚。全局选择器覆盖静态模型中的 `:root`、`html`、`body`、`*`（及单一全局复合选择器），完整复杂选择器仍为既有边界。真实样式表必须传入 PostCSS `from` 来源；无文件来源的语义夹具可独立模拟根/局部定义，不作为应用全表所有权校验入口。没有运行时并发、重试或持久化变化，相关维度不适用；未运行 WebView 像素测试。

## 验证记录

工作目录为仓库根目录，Node 为 `.nvmrc` 固定的 `24.18.0`。

- Red：新增 `sheetTokenBoundaries.test.ts` 的 30 项回归，`npm test -- src/styles/sheetTokenBoundaries.test.ts` 得到 21 个预期失败、9 个通过。
- Green：`npm test -- src/styles/sheetTokenBoundaries.test.ts src/styles/sheetTokenReferences.test.ts src/styles/sheetTokensSemantics.test.ts src/styles/sheetTokens.test.ts` 218/218 通过。矩阵映射：全局所有权 9 项（7 种选择器、媒体入口、合法对照），CSS-wide 拒绝 12 项（局部值 5、根值 3、祖先/全局继承来源 3、重要性/别名链 1），正常/恢复 9 项（文档根 unset 2、initial 1、媒体与选胜 1、普通属性 4、字符串 1）。
- 真实 glob 集成探针：临时新增无消费者的 `src/styles/.review-5280542926-probe.css`，内容为 `:root { --text-primary: 4px; }`；执行 `npm test -- src/styles/sheetTokens.test.ts` 得到 6 个预期失败、9 个通过，失败包含 `TOKEN_GLOBAL_OUTSIDE_SOURCE`，证明真实发现集携带来源并经过共同预检。探针随后删除；完整套件恢复绿色。
- 完整路由 `npm run format:check && npm run lint && npm run typecheck:strict && npm run build && npm test` 全部通过：151 文件 / 2060 项。首次严格类型检查指出 PostCSS `list.split` 第三个参数为必填，已显式传 `false` 保持原分隔语义后重跑通过；未跳过检查。`git diff --check` 通过。
- 文件/最长代码行（`wc -l` + TypeScript AST）：共享引擎 675 / 32、真实契约测试 850 / 66、新边界测试 172 / 58；既有语义测试 947 行只修正一处说明。无超硬上限。引擎超过 600 行讨论阈值，已复核所有权预检与值规范化均服务同一声明/取值模型，以两个小函数隔离选择器分类和来源验证；再拆模块会分散同一边界，保留当前内聚职责，无新增依赖。既有 66 行测试分组为平行契约案例；新增函数/分组均低于 60 行。圈复杂度为 `N/A — no configured complexity tool`，人工复核未新增深层控制流。
- 文档路由无配置自动行为检查，已结构化核对设计 §2.1、矩阵、错误码、真实来源接线及既有描述；数据模型未变。未改 Rust 源码，Rust 路由不适用；提交与推送仍分别刷新前端/Rust 覆盖率并执行 SonarQube。沿用上轮已验证的 `DEVELOPER_DIR=/Library/Developer/CommandLineTools`，仅为 Git 调用进程选择工具链，不改系统默认设置或接受 Xcode 许可；门禁最终结果与提交引用发布在原线程。
- 构建/门禁一次性产物结束后清理并恢复任务前已有产物；无关未跟踪文档 `2026-09-22-quality-assessment.md` 保持不变。

## 原线程处置依据

### [组件全局定义跨表覆盖](https://github.com/hailingu/PlotWeave/pull/288#discussion_r4073703017)

1. **处置** — 已修复，真实 P2 漏检，采用审查允许的拒绝组件全局定义方案。
2. **理由与变更** — 真实 glob 解析携带 `from`；`sheetDecls` 的 `assertGlobalTokenSource` 拒绝 `tokens.css` 之外的全局自定义属性（包括逗号分支、单一全局复合选择器和媒体块），报 `TOKEN_GLOBAL_OUTSIDE_SOURCE`。校验位于定义端，不依赖同表消费者和导入顺序；局部定义与全局普通样式保留。
3. **验证** — 9 项所有权正反例通过；真实 glob 注入无消费者全局覆盖的探针按预期拒绝，删除后聚焦 218 项与完整 2060 项通过，命令见上。
4. **后续** — 本条无剩余工作。未实现跨表完整级联；真实入口必须携带来源，无来源语义夹具只用于单独模拟，复杂选择器仍为既有模型边界。

### [自定义属性继承关键字](https://github.com/hailingu/PlotWeave/pull/288#discussion_r4073703025)

1. **处置** — 已修复，真实 P2 漏检，采用审查允许的明确拒绝方案。新证据说明既有“未建模”说明不能允许非根 unset 等值静默成为合法 color。
2. **理由与变更** — `isGuaranteedInvalid` 保留 initial/文档根 unset 的既有恢复语义，对非根 unset、inherit、revert/revert-layer 报 `TOKEN_CSS_WIDE_UNMODELED`；body/通配有继承来源，不能当作文档根初值。根/局部作用域共用此边界，重要性、别名链与媒体选胜不会绕过拒绝；普通属性与引号字符串不受影响。
3. **验证** — 12 项拒绝用例及 9 项正常/恢复对照通过，含有效/无效 fallback、真实悬空、根/局部和祖先入口、媒体环境与被覆盖声明。聚焦和完整验证结果同上。
4. **后续** — 本条无剩余工作。未实现 CSS-wide 自定义属性继承/回滚求值，遇到上述胜出值会明确失败；未运行 WebView 像素测试。
