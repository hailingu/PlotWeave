# PR #288 审查 5280077466 处理记录

审查：[5280077466](https://github.com/hailingu/PlotWeave/pull/288#pullrequestreview-5280077466)，基准提交 `40a63a67e28b58d4d2a8f791a71c5a6a431244dc`。本轮三项均提供了新的触发条件，属于该提交的一轮跟进；分类为 P2 契约漏检/误报，修复、验证、提交推送和原线程回复沿用已授权范围。第三项原例 `white-logo.svg` 被现有标识符边界排除，但 `white.svg` 与 URL 的 `#fff` 片段可复现同类误报，不能据原例不成立就关闭整个问题。

## 关键状态与不变量矩阵

| 前提/状态 | 动作/过渡 | 可观察结果 | 不变量与所有者/入口 | 验证 |
| --- | --- | --- | --- | --- |
| 组件规则内嵌规则（含隐式后代），或内嵌 at-rule | 结构/接线/类型扫描读取声明 | 明确报 `TOKEN_SHEET_NESTING_UNMODELED` | `sheetDecls` 必须覆盖声明或拒绝未建模布局，不能静默跳过；所有扫描入口共用 | 嵌套规则/媒体/条件、外层媒体与各消费入口的拒绝用例 |
| 普通平铺规则、外层 media 或 keyframes | 枚举声明 | 保留选择器、条件、源序与重要性；正常扫描 | 拒绝规则内嵌布局不能禁止已支持的外层容器 | 平铺/媒体/动画正例 |
| 局部自定义属性处于 supports/container/layer/keyframes 等非 media 上下文 | 建立局部定义表，含非活跃 media 或无消费点 | 明确报 `TOKEN_LOCAL_AT_RULE_UNMODELED` | `sheetDecls` 对局部定义先验证完整祖先链，不能将未知上下文当作无条件定义 | 各上下文与接线/类型拒绝用例 |
| media 中局部定义随浅深环境切换 | 解析消费点及 fallback | 活跃时使用定义，失活时回退；大小写 media 行为相同 | 已建模条件仍按环境生效；回退不绕过类型检查 | 上下文正例与恢复正反例 |
| URL/引号字符串含具名色、hex、函数色，或转义引号/括号 | 字面色检测 | 内容不被判为 CSS 颜色 | `hasColorLiteral` 仅检查颜色语法，忽略不透明字符串与 URL；结构扫描直接复用 | URL/字符串及转义边界用例 |
| URL/字符串与真实颜色、渐变/fallback 并存 | 字面色检测 | 外部真实颜色仍被识别 | 忽略区段不能吞掉其后的颜色成分 | 混合值正例与无真实颜色反例 |

本轮明确拒绝未建模嵌套布局和非 media 局部定义上下文，不实现完整 CSS 嵌套选择器展开或 supports/container/layer 求值。普通 at-rule 内无局部定义的非嵌套声明保留既有行为。没有运行时状态、并发、重试或持久化变更，相关维度不适用；WebView 像素测试不在本轮范围。

## 验证记录

工作目录为仓库根目录，Node 为 `.nvmrc` 固定的 `24.18.0`。

- Red：先增加 33 项语义用例，`npm test -- src/styles/sheetTokensSemantics.test.ts src/styles/sheetTokens.test.ts` 得到 23 个预期失败、139 个通过。失败涉及嵌套漏扫、未知局部上下文、局部大写 media 环境筛选和 URL/字符串颜色误报；原始 `white-logo.svg` 反例保持通过。
- Green：同一聚焦命令 162/162 通过。5 个嵌套布局、5 个未知局部条件用例分别验证声明枚举、局部定义、接线与类型四个实际入口；平铺/外层媒体/动画正例 1 项、浅深环境与有效/无效 fallback 2 项、无消费点和无局部定义对照 1 项；字面色不透明内容 12 项、外部真实颜色 7 项。共新增 33 项，均覆盖矩阵所选状态。
- 完整路由 `npm run format:check && npm run lint && npm run typecheck:strict && npm run build && npm test` 全部通过，149 文件 / 2004 测试；`git diff --check` 通过。未改后端，Rust 路由不适用；提交与推送的版本化钩子仍各自刷新前端/Rust 覆盖率并执行 SonarQube，最终结果与提交链接发布到原线程。
- 首次 pre-commit 被 SonarQube `typescript:S2310` 拦截：URL 扫描器在 for 循环体内修改游标。已改为显式 while 游标，保持转义消费语义，重跑聚焦与完整前端路由通过后重新执行门禁；未绕过钩子或抑制规则。
- 文件/函数上限：共享引擎 631 行 / 最长 32 代码行，颜色值模块 202 / 28，语义测试 947 / 66，真实契约测试 843 / 66。通过 `wc -l` 与 TypeScript AST 检查，不超过硬上限。引擎超过 600 行讨论阈值，复核后保留在同一声明/取值模型：新增上下文预检直接保护既有遍历入口，拆出另一个依赖 PostCSS 父链的小模块会分散同一模型约束；颜色区段识别归既有 `cssColorContract`，未增加依赖。既有 66 行测试分组保持同一契约的并列案例，本轮新分组均低于 60 行；圈复杂度为 `N/A — no configured complexity tool`，人工复核未引入深层控制流。
- 文档路由无配置自动行为检查，已结构化核对设计 §2.1、测试头注、错误码与本文矩阵；未改变数据模型。保留完整 CSS 文法、复杂选择器与 WebView 像素实测缺口，本轮不宣称支持原生嵌套或未知条件求值。
- 构建与门禁的一次性产物结束后清理，恢复任务前已有产物；无关 `docs/reviews/2026-09-22-quality-assessment.md` 保持不变。

## 原线程处置依据

### [嵌套组件规则](https://github.com/hailingu/PlotWeave/pull/288#discussion_r4073291851)

1. **处置** — 已修复，真实 P2 漏检，采用审查允许的明确拒绝方案。
2. **理由与变更** — `sheetDecls` 在输出任何声明前调用 `assertSheetContexts`，对规则内部的子规则或 at-rule 报 `TOKEN_SHEET_NESTING_UNMODELED`，避免结构、接线和类型扫描各自遗漏同一输入；平铺规则及外层容器保持原行为。
3. **验证** — 5 个嵌套反例在四个消费入口均明确报错，普通规则/媒体/keyframes 正例保持上下文及源序；聚焦 162 项与完整 2004 项通过。
4. **后续** — 本条无剩余工作。原生 CSS 嵌套仍未建模，使用前需另行扩展模型；未进行 WebView 像素验证。

### [未知条件局部定义](https://github.com/hailingu/PlotWeave/pull/288#discussion_r4073291866)

1. **处置** — 已修复，真实 P2 漏检，明确拒绝未建模上下文。
2. **理由与变更** — `assertLocalContext` 在局部定义收集前检查完整祖先链，非 media 上下文报 `TOKEN_LOCAL_AT_RULE_UNMODELED`，包括非活跃媒体内的未知块或无消费者定义。`conditionActive` 同时按大小写不敏感识别已支持 media，避免 `@MEDIA` 定义在错误环境生效。
3. **验证** — supports/container/layer/keyframes 与非活跃 media 嵌套拒绝、无消费者拒绝、无局部定义对照，以及小写/大写 media 的成功/失败回退用例通过；聚焦与完整路由结果同上。
4. **后续** — 本条无剩余工作；完整条件求值仍未建模。普通非 media 容器中无局部定义的声明继续保留扫描，不代表实现了浏览器条件求值。

### [URL/字符串中的颜色词](https://github.com/hailingu/PlotWeave/pull/288#discussion_r4073291880)

1. **处置** — 已修复，P2 误报；原始文件名例子不复现，但问题类型由相邻合法输入证实。
2. **理由与变更** — `hasColorLiteral` 经 `colorSyntaxOf` 跳过 URL 与引号字符串，`opaqueEnd` 保留转义引号/括号边界，并用空格占位避免删除后拼成新标识符。URL 外的函数、渐变和 fallback 色值继续检测。
3. **验证** — 12 个不透明内容反例及 7 个外部真实颜色正例通过，包含 quoted/unquoted URL、大小写 URL、数据 URL、hex/函数色片段、image-set、转义边界及混合值；原始 `white-logo.svg` 用例一并保留。聚焦与完整路由结果同上。
4. **后续** — 本条无剩余工作；该扫描仅负责字面颜色识别，不实现完整 CSS 值文法验证。
