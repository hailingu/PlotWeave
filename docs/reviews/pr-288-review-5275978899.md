# PR #288 第二轮审查处理记录

审查：[5275978899](https://github.com/hailingu/PlotWeave/pull/288#pullrequestreview-5275978899)，基准提交 `039d78d9bac7842b1079fe69c7788988a181b9e3`。本轮范围为修复、验证、提交推送和原线程回复。

三条意见均为新的触发证据，属于当前提交的一轮跟进审查。按仓库分级均为 P2 契约测试缺陷，无已证实的 P1 用户数据损失风险。第一条关于最低展示色声明数的结论成立，但其“源码被当作不透明文本”的表述不准确：原实现使用 PostCSS AST，缺陷在于对每个文件施加了不必要的最低声明数要求。

## 关键状态与不变量矩阵

所有者：`sheetTokens.test.ts` 的发现、接线和展示色类型扫描入口；根令牌、局部变量、逗号选择器分支和媒体环境均使用相应共用解析路径。

| 前提/状态 | 动作/过渡 | 可观察结果 | 不变量 | 验证 |
| --- | --- | --- | --- | --- |
| 新增只有布局、动画或空内容的样式表 | 自动发现并扫描 | 正常纳入扫描，没有展示色也通过 | 是否加入契约不依赖颜色声明数量 | 临时真实 CSS 文件 Red/Green 探针与空展示色扫描夹具 |
| 主变量有效，fallback 内含缺失变量 | 按环境与分支选择实际取值 | 不报告未选中的缺失变量 | 接线只要求实际取值路径可解析，备用路径仍参与变量环判定 | 根令牌、局部别名和嵌套 fallback 正例 |
| 主变量缺失、initial、断链、循环或仅部分环境有效 | 选择 fallback 并继续解析 | 有效回退通过，无效回退点名 | 不能以忽略备用分支为由放过被选中的失败路径 | 失败、恢复、媒体环境和选择器分支用例 |
| 纯颜色消费点获得颜色加尺寸/裸数值/多余词形 | 对完整顶层值校验 | 报告不相容；合法完整颜色通过 | 含有一个颜色成分不等于整条颜色值有效 | 根令牌/局部别名及颜色函数正反例 |
| 边框、文字阴影或多值边框颜色合法使用多个成分 | 按已有属性类别校验 | 继续通过 | 纯颜色约束不误施加到合法简写与颜色列表 | 合法简写与边框颜色列表用例 |

无运行时异步、并发、重试或持久化变更，这些维度不适用。完整 CSS 文法、函数内部参数合法性、复杂选择器及跨选择器特异性仍为既有静态模型边界；不承诺替代浏览器引擎。真实 WebView 像素验证不在本轮内。

## 验证记录

工作目录为仓库根目录，使用 `.nvmrc` 固定的 Node `24.18.0`。

- Red：先添加真实临时样式表（仅布局与动画）并运行发现测试，按预期因“无展示色声明”失败。再添加接线与完整颜色值用例，`npm test -- src/styles/sheetTokens.test.ts` 得到 14 个预期失败、61 个通过；失败对应最低声明数、未选 fallback 误报及完整纯颜色值漏检。
- Green：修复后，同一聚焦命令 75/75 通过（新增 24 个用例），此时真实临时样式表仍在发现集中；随后删除探针。接线实际取值路径 7 个用例、完整颜色值与无展示色扫描 17 个用例全部通过，覆盖上表的正常、失败及回退路径。原有断链、变量环、局部遮蔽与全环境用例保持通过。
- 路由检查：`npm run format:check && npm run lint && npm run typecheck:strict && npm run build && npm test` 全部通过；148 个测试文件、1917 个用例通过。
- `git diff --check` 通过。将既有颜色词形与属性类型校验抽到 `src/styles/cssColorContract.ts`，避免增加重复解析器并遵守文件上限；测试文件 1661 物理行，纯值模块 162 行。以 TypeScript AST 确定函数边界并剔除纯注释行后，测试最长可执行单元 66 代码行，纯值模块最长函数 24 行，均在 80 行内。
- `docs/**` 无配置自动行为检查；已结构化核对设计 §2.1、测试头部矩阵、本文、三条原意见和保留边界的一致性。未改变数据模型、应用运行时或依赖。
- Rust 路由不适用，因为没有后端代码变更。提交和推送分别由版本化钩子运行共享静态检查、新鲜前端/Rust 覆盖率与 SonarQube；最终提交编号及门禁结果记录在下面的原审查线程中。
- 首次提交门禁报告 `typescript:S5869`：抽出的颜色正则启用了忽略大小写，`a-f` 与 `A-F` 范围重复。移除重复大写范围，保留相同识别语义，再运行聚焦测试及完整门禁；未绕过钩子或添加规则抑制。
- 未运行真实 WebView 像素验证，因为本轮改变的是静态契约扫描行为；完整 CSS 文法与浏览器层叠边界见矩阵说明。构建产物和门禁覆盖率的临时输出在完成后清理，任务前已有产物予以恢复。

## 原线程处置依据

以下记录对应包含本文的修复提交；发布原线程回复时补充不可变提交链接和最终门禁结果。

### [每表最低展示色声明数](https://github.com/hailingu/PlotWeave/pull/288#discussion_r4069950141)

1. **处置** — 已修复，真实缺陷，按仓库标准归为 P2。合法样式表会造成契约误报，但没有 P1 用户数据损失触发证据。
2. **理由与变更** — 删除每表必须包含展示色声明的断言；保留根令牌源和组件发现集存在的检查。扫描本身继续基于 PostCSS AST，新增样式表自动纳入发现集，布局、动画及空表不再被附加颜色数量要求。
3. **验证** — 真实临时 CSS 探针先失败后通过；布局、动画及空表语义夹具通过；75 项聚焦测试和完整路由 1917 项测试通过，命令见上。
4. **后续** — 本条无剩余工作。

### [未选中的 fallback 误报](https://github.com/hailingu/PlotWeave/pull/288#discussion_r4069950151)

1. **处置** — 已修复，真实缺陷，P2。
2. **理由与变更** — 用 `unresolvedValueRefs` 遍历实际选中路径，移除平面正则提取及重复接线解析器。接线与类型扫描共用 `valueScopeIn` 和 `resolveDisplayValue`；有效主值的备用路径不报悬空，主值失效后递归进入 fallback，变量环仍计算备用路径依赖。实际全表扫描与夹具均调用 `unresolvedRefsIn`。
3. **验证** — 主值有效、局部别名、嵌套回退、initial、主值与回退都缺失、循环恢复及媒体/选择器分支切换 7 个用例通过；原有断链与全环境扫描保持通过。聚焦及完整路由结果见上。
4. **后续** — 本条无剩余工作。复杂选择器和跨选择器特异性仍为既有静态模型边界。

### [完整纯颜色值校验](https://github.com/hailingu/PlotWeave/pull/288#discussion_r4069950159)

1. **处置** — 已修复，真实缺陷，P2。
2. **理由与变更** — `colorTypeOk` 对纯颜色属性调用完整顶层值校验：颜色前后多余尺寸、裸数值及词形不再因包含颜色成分而通过。合法单颜色函数、边框颜色列表、SVG paint 关键字和含尺寸的边框/文字阴影简写保留；纯值逻辑抽到 `cssColorContract.ts` 并由原全表扫描使用。
3. **验证** — 15 组属性/值正反用例以及根级别名 `var(--text-primary) 4px` 消费场景通过；聚焦及完整路由结果见上。
4. **后续** — 本条无剩余工作。颜色函数内部参数与完整简写文法仍为已披露边界，不承诺完整 CSS 文法校验。
