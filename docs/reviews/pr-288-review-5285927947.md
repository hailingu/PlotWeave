# PR #288 审查 5285927947 处理记录

审查：[5285927947](https://github.com/hailingu/PlotWeave/pull/288#pullrequestreview-5285927947)，基准 `06924ed52ee8cb4cdf429fc787147fbe4f4c9cf5`。两项均为 P2，属于该提交唯一的一轮跟进：

- 第一项是新入口：`-webkit-text-stroke` 没有进入展示色分类。
- 第二项是新证据：[审查 5280837519](pr-288-review-5280837519.md) 把背景简写与长形合为一组取胜，这会误判合法规则，因此本轮修正那次处置。

之后再有非 P1 发现按轮次预算关闭，交由仓库所有者裁决。

## 关键状态与不变量矩阵

| 前提/状态 | 动作/过渡 | 可观察结果 | 不变量与所有者/入口 | 验证 |
| --- | --- | --- | --- | --- |
| 组件表在 `-webkit-text-stroke` 上写字面色，或把尺寸令牌放在颜色位置（`1px var(--radius-sm)`） | 结构/类型扫描 | 字面色按未审计违例点名，`1px 4px` 按类型不相容点名 | 带颜色的简写不能绕过全表契约；所有者 `isDisplayColorProp` 与 `colorTypeOk` | 分类正反例（含 `-webkit-text-stroke-width` 反例）、类型夹具、真实 glob 探针 |
| `-webkit-text-stroke` 为「宽度 \|\| 颜色」合法形态（`1px var(--text-primary)`、`var(--text-primary) thin`、仅宽度、CSS-wide 关键字） | 类型扫描 | 通过 | 至多一个宽度和一个完整颜色成分；多余词形、第二个宽度或渐变拒绝 | 类型夹具 3 反 3 正 |
| 危险规则 `background: var(--danger)` 之后只写 `background-image: none` | 黄金接线 | 通过：颜色仍为 `var(--danger)`，图像为 `none` | 简写展开到颜色与图像两个成分，各成分独立层叠；长形只覆盖自身成分 | 取胜夹具（审查所举原例） |
| 后位或带 `!important` 的 `background-color`，或叠加 `background-image`/含图像的简写 | 黄金接线 | 覆盖颜色或叠加图像即判违例 | 有效颜色须为 `var(--danger)`，有效图像须为 `none` | 取胜夹具 5 项 |
| 只有 `background-color: var(--danger)`，或纯色简写被后位 `background-color: var(--danger)` 覆盖 | 黄金接线 | 通过 | 纯色简写只贡献颜色层：全部 8 个配对环境都消解为单一颜色时，图像为初始值 `none` | 取胜夹具 2 项 |

范围说明：只有能在全部配对环境消解为单一颜色的简写才按「纯颜色层」展开；具名色或其他复杂简写会保守地判为图像成分，从而明确失败。背景的其余长形（位置、尺寸、重复等）不影响配对，未建模。`-webkit-text-stroke` 的宽度只识别常用长度单位与 `thin`/`medium`/`thick`。跨规则特异性仍由「唯一且无条件规则」前提约束。没有运行时并发、重试或持久化变化，相关维度不适用；未运行 WebView 像素测试。

## 验证记录

工作目录为仓库根目录，Node 为 `.nvmrc` 固定的 `24.18.0`。

- Red：先把 `backgroundPaint` 等价改写为返回颜色/图像成分（仍按旧的单组取胜），再运行 `npx vitest run src/styles/sheetTokenBoundaries.test.ts src/styles/sheetTokens.test.ts`，得到 3 个预期失败、71 个通过：
  - `-webkit-text-stroke` 分类未识别；
  - 类型扫描没有报出任何错误；
  - 成分夹具中，重要图像长形吞掉了颜色成分。
- Green：`npx vitest run src/styles` 共 247/247 通过。
- 真实 glob 探针：临时新增 `src/styles/.review-5285927947-probe.css`，写入 `-webkit-text-stroke: 1px #fff` 与 `1px var(--radius-sm)` 两条声明；前者被点名为未审计字面色，后者在各环境报类型不相容 `1px 4px`。探针已删除。
- 完整路由 `npm run format:check && npm run lint && npm run typecheck:strict && npm run build && npm test` 全部通过：151 个文件、2089 项。`git diff --check` 通过，构建产生的 `dist/` 已删除。
  - 文件行数：`cssColorContract.ts` 200、引擎 721、真实契约测试 983、边界测试 305，均低于上限。
  - 新增函数均低于 25 行；圈复杂度未配置工具，结果为 `N/A — no configured complexity tool`。
  - 提交与推送门禁的结果发布在原线程。
- 文档路由没有配置自动检查，已结构化核对 `docs/ui-design.md` §2.1 与测试头注；未改 Rust 源码，Rust 路由不适用。

## 原线程处置依据

### [-webkit-text-stroke 分类](https://github.com/hailingu/PlotWeave/pull/288#discussion_r4078250414)

已修复，真实 P2 漏检。该简写加入展示色分类，并按「宽度 \|\| 颜色」文法专门校验。

### [背景长形独立层叠](https://github.com/hailingu/PlotWeave/pull/288#discussion_r4078250416)

已修复，真实 P2 误报，由上一轮处置引入。改为把简写展开到颜色与图像成分，各成分独立层叠，替代之前的单组取胜。
