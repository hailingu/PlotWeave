# PR #288 审查 5285788301 处理记录

审查：[5285788301](https://github.com/hailingu/PlotWeave/pull/288#pullrequestreview-5285788301)，基准 `0b8060116aefd2a07b9868ab5dbb75f94c2a89cb`。唯一一项是新 P2 漏检（新入口：根令牌源中的选择器列表），属于该提交唯一的一轮跟进。之后再有非 P1 发现按轮次预算关闭，交由仓库所有者裁决。

## 关键状态与不变量矩阵

| 前提/状态 | 动作/过渡 | 可观察结果 | 不变量与所有者/入口 | 验证 |
| --- | --- | --- | --- | --- |
| 根令牌源中，基线 `:root` 之后出现 `:root, :host`、`:host, :root` 或 `:ROOT, .x` 上的同名定义 | `tokenValuesOf` 取值 | 该定义按源序与重要性参与根令牌取胜 | 文档根上生效的定义不能被取值跳过；所有者 `hasRootBranch`，`tokenValuesOf` 与 `assertRootContexts` 共用 | 3 项夹具；真实 `tokens.css` 探针报出类型不相容 |
| 列表根分支位于 media 或未知 at-rule 内 | 取值与上下文预检 | media 按环境取值；`@supports` 等报 `TOKEN_ROOT_AT_RULE_UNMODELED` | 列表根分支与单独 `:root` 受同一上下文规则约束 | 浅/深两环境 + 未知上下文 1 项 |
| `html`、`body`、`*`、`:root.dark`、`:root, html`、`:is(:root)`、`:root .child` 上的自定义属性 | 上下文预检 | 报 `TOKEN_ROOT_SELECTOR_UNMODELED` | 可能命中或覆盖文档根、但特异性或继承尚未建模的选择器必须显式失败，不能静默忽略 | 7 项夹具；真实 `tokens.css` 探针 |
| 非根选择器（`.other, :host`）上的自定义属性，或 `*`、`html` 上的普通属性 | 取值 | 根值不变，不报错 | 只约束可能到达文档根的自定义属性；真实 `tokens.css` 中的 `* { backdrop-filter }` 保持有效 | 对照夹具；完整套件 |

范围说明：只对单独 `:root` 与列表中的 `:root` 分支取值；`html`、`:root.dark` 等选择器的特异性与层叠未实现，出现时会明确失败。组件表的全局所有权规则（`TOKEN_GLOBAL_OUTSIDE_SOURCE`）不变。没有运行时并发、重试或持久化变化，相关维度不适用；未运行 WebView 像素测试。

## 验证记录

工作目录为仓库根目录，Node 为 `.nvmrc` 固定的 `24.18.0`。

- Red：`npx vitest run src/styles/sheetTokenBoundaries.test.ts` 得到 11 个预期失败、43 个通过（列表根分支 3 项、媒体与上下文 1 项、未建模选择器 7 项；非根对照项通过）。
- Green：`npx vitest run src/styles` 共 244/244 通过。
- 真实令牌源探针：先备份 `src/styles/tokens.css`，再临时追加 `:root, :host { --text-primary: 4px; }`；`npx vitest run src/styles/sheetTokens.test.ts` 点名 `.pw-dialog-text color: 4px` 等类型不相容值。改为追加 `html { --text-primary: 4px; }` 后报 `TOKEN_ROOT_SELECTOR_UNMODELED`。之后已从备份还原，`git diff --quiet src/styles/tokens.css` 通过。
- 完整路由 `npm run format:check && npm run lint && npm run typecheck:strict && npm run build && npm test` 全部通过：151 个文件、2086 项。`git diff --check` 通过，构建产生的 `dist/` 已删除。引擎 720 行、边界测试 285 行，均低于上限；新增函数均低于 20 行。圈复杂度：未配置复杂度工具，结果为 `N/A — no configured complexity tool`。提交与推送门禁的结果发布在原线程。
- 文档路由没有配置自动检查，已结构化核对 `docs/ui-design.md` §2.1 与测试头注；未改 Rust 源码，Rust 路由不适用。

## 原线程处置依据

### [选择器列表中的 :root 分支](https://github.com/hailingu/PlotWeave/pull/288#discussion_r4078128254)

已修复，真实 P2 漏检，同时采用审查建议的两种方案。列表中的 `:root` 分支按根规则取值，并受同一上下文预检约束；其他可能命中文档根、但未建模的选择器上的自定义属性报 `TOKEN_ROOT_SELECTOR_UNMODELED`。
