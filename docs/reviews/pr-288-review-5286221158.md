# PR #288 审查 5286221158 处理记录

审查：[5286221158](https://github.com/hailingu/PlotWeave/pull/288#pullrequestreview-5286221158)，基准 `8d53a7f184204910a8007eac5838496187d25b7c`。这是该提交唯一的一轮跟进，两项均为 P2：

- 第一项（CSS 注释未屏蔽）是误报：真实入口拿到的值已由 PostCSS 剥离注释。
- 第二项（遮罩简写绕过渐隐契约）有新入口，属于真实漏检。

之后再有非 P1 发现，按轮次预算关闭，交由仓库所有者裁决。

## 关键状态与不变量矩阵

| 前提/状态 | 动作/过渡 | 可观察结果 | 不变量与所有者/入口 | 验证 |
| --- | --- | --- | --- | --- |
| 组件表写 `mask`/`-webkit-mask` 简写，或写 `mask-border(-source)`、`-webkit-mask-box-image(-source)` | 全表遮罩契约扫描 | 抛出 `MASK_IMAGE_UNMODELED`，点名表、选择器和属性 | 所有承载遮罩图像的属性都必须进入契约；未建模的形态明确失败。所有者：`isMaskImageProp`、`expectMaskFade` | 6 个属性夹具；真实 glob 探针 |
| `mask-image`/`-webkit-mask-image` 长形为 linear-gradient | 同上 | 按首末全透明、内部全不透明判定 | 已建模路径行为不变 | 既有全表断言与剖析夹具 |
| 尺寸、模式等不承载图像的遮罩长形（`mask-size`、`mask-border-width` 等） | 同上 | 不进入契约 | 只按是否承载图像分类 | 5 个反例夹具 |
| 声明值内含注释（`var(--text-primary /* var(--old) */)`、`/* white */ var(...)`） | 实际解析后做接线、类型与字面色扫描 | 不报悬空、类型错误或字面色 | 所有扫描入口都消费 PostCSS `decl.value`，注释已被剥离；`maskCssOpaque` 头注写明这一前提 | 实际解析回归夹具 |

范围说明：

- 遮罩简写的图像成分没有解析，一律明确拒绝。真实样式表目前只使用 `mask-image` 长形。
- 没有运行时并发、重试或持久化变化，这些维度不适用。
- 未运行 WebView 像素测试。

## 验证记录

工作目录为仓库根目录，Node 为 `.nvmrc` 固定的 `24.18.0`。

- 探针：临时文件 `src/styles/review-5286221158-probe.test.ts`，已删除。实际解析后，`decl.value` 分别为 `var(--text-primary )` 和 `var(--text-primary)`，注释已剥离；接线扫描与类型扫描均返回 `[]`。
- Red：先把遮罩循环等价抽取为 `isMaskImageProp`/`expectMaskFade`，再运行 `npx vitest run src/styles`，结果为 6 失败、258 通过。
  - 6 个失败都是承载图像的遮罩属性被跳过。
  - 注释回归夹具直接通过，确认第一项为误报。
- Green：`npx vitest run src/styles` 共 264/264 通过。
- 真实 glob 探针：临时新增 `src/styles/review-5286221158-probe.css`，写入 `mask: linear-gradient(#000, #000)`，全表契约报出 `MASK_IMAGE_UNMODELED`。探针已删除。
- 完整路由 `npm run format:check && npm run lint && npm run typecheck:strict && npm run build && npm test` 全部通过，共 2106 项。
  - `git diff --check` 通过，构建产生的 `dist/` 已删除。
  - 文件行数：真实契约测试 1031、边界测试 320，均低于测试文件上限。
- 未改 Rust 源码，Rust 路由不适用。

## 原线程处置依据

### [CSS 注释屏蔽](https://github.com/hailingu/PlotWeave/pull/288#discussion_r4078491487)

不改代码，属于 P2 误报。

- PostCSS 解析时会从 `decl.value` 剥离值内注释。
- 所有扫描入口（`sheetDecls`、`tokenValuesOf`、字面色扫描）都只消费 `decl.value`。
- 补了实际解析回归夹具，并在 `maskCssOpaque` 头注写明这一前提。

### [遮罩简写](https://github.com/hailingu/PlotWeave/pull/288#discussion_r4078491494)

已修复，真实 P2 漏检。

- 按结构式识别所有承载遮罩图像的属性。
- 只有 `mask-image` 长形的 linear-gradient 已建模，其余形态明确失败。
