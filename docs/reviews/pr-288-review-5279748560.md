# PR #288 审查 5279748560 处理记录

审查：[5279748560](https://github.com/hailingu/PlotWeave/pull/288#pullrequestreview-5279748560)，基准提交 `21fd2ac31073afaaf734df7b1019b177793f4fc2`。两条均为新的 P2 契约漏检，属于当前提交的一轮跟进审查，范围为修复、验证、提交推送及原线程回复。

## 关键状态与不变量矩阵

| 前提/状态 | 动作/过渡 | 可观察结果 | 不变量与所有者/入口 | 验证 |
| --- | --- | --- | --- | --- |
| 元素自身与祖先定义同名变量，祖先为后位或 important | 在消费分支选值 | 自身指定值优先；无效颜色被点名，合法值通过 | `valueScopeIn` 先定位最近定义元素，再在该元素上按重要性/源序取胜；接线与类型共用 | 自身/祖先顺序、后代/子代及有效/无效消费用例 |
| 没有自身定义，多个祖先或根级定义可达 | 沿已支持的简单选择器继承路径取值 | 最近祖先优先于远祖先的重要值；同元素重要性仍有效 | 继承层级先于重要性，复合选择器的同元素延续不能误当新祖先 | 多层祖先、全局根/通配、复合选择器用例 |
| 自身定义仅部分媒体环境活跃，或逗号分支定义不同 | 按环境与分支选值 | 无活跃自身定义时继承；其他分支独立取值 | 未生效定义不遮蔽，分支间不共享自身定义 | 媒体与逗号分支用例 |
| 最近定义为 initial/断链，消费点使用 fallback | 选胜后解析或回退 | 不用远祖先救回被遮蔽的值；有效回退恢复，无效回退拒绝 | 选胜规则对接线和类型失败/恢复路径一致 | 悬空诊断与 fallback 正反例 |
| 根令牌位于非 media at-rule（含嵌套、非活跃 media 内），或 at-rule 反向嵌在根规则内部 | 进入 `tokenValuesOf` | 显式报错，不返回忽略该入口的根值映射；根规则内部嵌 media 的声明布局也拒绝 | 根定义必须位于模型支持的上下文（media 包住根规则）；诊断码 `TOKEN_ROOT_AT_RULE_UNMODELED` 是该入口的错误契约 | supports/layer/container 与两种嵌套方向的拒绝用例 |
| 根令牌位于已支持 media，或其他 at-rule 不含根自定义属性 | 遍历根定义 | 活跃 media 正常选值；无根定义的其他规则不影响根值 | 拒绝范围绑定根令牌上下文，不给无关 CSS 附加限制 | 支持环境及无根定义正例 |

无运行时并发、重试或持久化变化，这些维度不适用。本轮仅扩展既有简单后代/子代选择器的继承优先级；复杂选择器、同元素跨选择器特异性和完整 CSS 层叠仍未建模。未知根 at-rule 或根规则内嵌 at-rule 的声明布局选择显式拒绝而非假设浏览器支持；不引入完整 supports/container/layer 求值器。真实 WebView 像素验证不在本轮范围。

## 验证记录

工作目录为仓库根目录，使用 `.nvmrc` 固定的 Node `24.18.0`。

- Red：先添加 27 个语义用例，`npm test -- src/styles/sheetTokensSemantics.test.ts src/styles/sheetTokens.test.ts` 得到 22 个预期失败、105 个通过。继承用例包含自身声明在祖先前/后两个顺序，保证祖先重要性和源序均不能越过继承层级；fallback 正例同时断言实际消费值，避免因错误取得祖先颜色而假通过。
- 相邻入口：新增“未知条件嵌在根规则内部”的用例，`npm test -- src/styles/sheetTokensSemantics.test.ts -t '根规则内部嵌套未知条件'` 先得到 1 个预期失败；补齐该上下文遍历后通过。最终入口复核再将根内嵌 media 纳入相同错误契约，观察该新增用例先失败；明确拒绝未建模的反向嵌套布局后通过，已支持的 media 包住根规则形式保留。
- Green：两套聚焦测试 129/129 通过；相对基准新增 29 个用例（继承/恢复 18 个，根上下文 11 个）。接线与类型均通过 `valueScopeIn` 选取最近定义，根令牌真实入口 `tokenValues` 调用受同一预检保护的 `tokenValuesOf`。
- 完整路由：`npm run format:check && npm run lint && npm run typecheck:strict && npm run build && npm test` 全部通过；149 个测试文件、1971 个测试通过。首次严格类型检查发现 PostCSS 父链还可能包含 Document，已显式使用完整 `AnyNode` 联合类型后重跑通过，没有断言或跳过该检查。
- `git diff --check` 通过。`wc -l`/TypeScript AST 检查：真实契约测试 840 行、共享引擎 604 行、语义测试 796 行；最长可执行单元分别为 66、32、66 代码行，无超过文件或函数硬上限。共享引擎超过 600 行的讨论阈值，已复核职责：根上下文验证、继承距离及最近定义筛选均直接服务同一静态取值模型，并已有独立小函数；另拆文件会把同一选择器/环境规则分散，不改善边界。新增函数分别承担这些职责；没有复制解析器或增加依赖。既有 66 行测试分组保持同一契约的平行案例，圈复杂度为 `N/A — no configured complexity tool`，控制流复核未新增深层嵌套。
- `docs/**` 无配置自动行为检查；已结构化核对设计 §2.1、测试头部矩阵、本文与两条审查的触发条件、错误契约和保留边界。无后端代码变化，Rust 路由不适用；提交与推送的版本化钩子仍分别刷新前端/Rust 覆盖率并运行 SonarQube，最终结果与提交链接记录在原线程。
- 构建与门禁临时输出完成后清理并恢复任务前已有产物；无关未跟踪文档 `2026-09-22-quality-assessment.md` 保持不变。

## 原线程处置依据

### [自身指定值与祖先继承值](https://github.com/hailingu/PlotWeave/pull/288#discussion_r4073009057)

1. **处置** — 已修复，真实缺陷，P2。该反例涉及继承优先级，不能仅以跨选择器特异性未建模来关闭。
2. **理由与变更** — `valueScopeIn` 先筛选当前环境可达定义，再由 `nearestDefinitions` 根据既有简单后代/子代链定位最近定义元素，最后在同元素候选中应用 `pickWinner`。自身指定值及近祖先的值不受远祖先重要性或后位源序压制；复合选择器延续和通配声明仍作为同元素候选。
3. **验证** — 18 个继承/恢复用例通过，覆盖源序、子代/后代、根级来源、多层祖先、通配、同元素重要性、媒体环境、逗号分支、无效近值的悬空诊断及有效/无效 fallback。聚焦 129 项、完整 1971 项测试通过，命令见上。
4. **后续** — 本条无剩余工作；复杂选择器和同元素跨选择器特异性仍为既有边界，未运行 WebView 像素验证。

### [非 media 根令牌上下文](https://github.com/hailingu/PlotWeave/pull/288#discussion_r4073009066)

1. **处置** — 已修复，真实缺陷，P2，采用意见允许的“显式拒绝未建模上下文”方案。
2. **理由与变更** — `assertRootContexts` 检查根自定义属性的祖先链，对未知非 media at-rule 抛出 `TOKEN_ROOT_AT_RULE_UNMODELED`。验证先于按环境求值，未知块即使嵌在非活跃媒体也不能被默默跳过；根规则内部嵌任何 at-rule 的声明布局（包括 media）尚未建模，同样明确拒绝；不含根自定义属性的其他规则不受限制。已支持的 media 名称按大小写不敏感处理。
3. **验证** — supports/layer/container、非活跃媒体嵌套、根规则内反向嵌套拒绝，以及无根令牌的其他规则与大小写 media 正例，共 11 项通过；聚焦和完整路由结果见上。
4. **后续** — 本条无剩余工作。supports/container/layer 的浏览器求值仍未建模，新增这类根令牌定义会明确失败，必须另行扩展模型后使用。
