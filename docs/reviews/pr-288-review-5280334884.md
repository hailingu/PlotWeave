# PR #288 审查 5280334884 处理记录

审查：[5280334884](https://github.com/hailingu/PlotWeave/pull/288#pullrequestreview-5280334884)，基准提交 `7e3a2e5c6d7ae4d385fff79612ae0ab6915f62fe`。一条新 P2 误报：字符串中的 `var()` 字样被加入变量依赖图，合法字面值被判自环。它不同于上一轮字面颜色识别，属于该提交的一轮跟进审查；在已授权范围内修复、验证、提交推送并回复原线程。

## 关键状态与不变量矩阵

| 前提/状态 | 动作/过渡 | 可观察结果 | 不变量与所有者/入口 | 验证 |
| --- | --- | --- | --- | --- |
| 局部或根变量值为包含 var 字样的引号字符串 | 收集依赖、解析 content 消费 | 无假依赖/自环/悬空，返回原样字符串 | 依赖图与取值必须只解释真实 CSS var 函数；`allVarRefs`、`variableCycles`、`unresolvedValueRefs`、`resolveDisplayValue` 共用词法边界 | 原审查、引号/转义/括号及根/局部入口正例 |
| URL 内容含 var 字样或不完整函数文本 | 枚举引用及解析图像值 | 内容保留，不补查 URL 内的变量 | 不透明内容不成为依赖或替换对象，URL 外真实引用仍有效 | quoted/unquoted/data URL 与混合值用例 |
| 字符串/URL 与真实引用、真实缺失名共存 | 解析前后缀与值链 | 只替换真实引用，真实缺失名仍报告 | 词法遮罩保持偏移，不能吞掉后续 token 或改变原值 | 混合值、两个消费位置、简易 resolveChain 入口 |
| 主值缺失或真实循环，fallback 含引号/URL/真实引用 | 选择回退并递归求值 | 字面回退恢复；真实循环保持无效，真实回退引用参与图 | 回退的括号边界不受引号/URL 内容干扰，真实依赖仍参与环检测 | 字面 fallback、真循环及选中/未选中路径正反例 |
| 字符串被用于纯颜色属性 | 解析后类型检查 | 保留字符串后仍报类型不相容 | 忽略伪引用不能让无效颜色通过 | 颜色消费反例 |

本轮重用并提取上一轮不透明区段识别到 `cssValueSyntax.ts`，给颜色识别和变量扫描共用。屏蔽只用于识别，消费结果由原始值切片组装，避免吞掉字面内容；功能边界仍为已建模 CSS 子集，不实现完整 CSS 文法或选择器层叠。无运行时并发、重试、持久化变化，相关维度不适用；未运行 WebView 像素测试。

## 验证记录

工作目录为仓库根目录，Node 为 `.nvmrc` 固定的 `24.18.0`。

- Red：先新增 `sheetTokenReferences.test.ts` 的 26 个用例，`npm test -- src/styles/sheetTokenReferences.test.ts` 得到 24 个预期失败、2 个通过。覆盖原审查假自环以及依赖图之外的解析、悬空检查、根变量、URL 与 fallback 入口。
- Green：`npm test -- src/styles/sheetTokenReferences.test.ts src/styles/sheetTokensSemantics.test.ts src/styles/sheetTokens.test.ts` 188/188 通过。新用例按矩阵映射：局部不透明内容 9 项、根入口 2 项、混合值 4 项、真实悬空 2 项、简易链 1 项、字符串误用作颜色 1 项、字面 fallback 5 项、真实直接/间接循环 2 项。根夹具首次绿色阶段暴露了无分号自定义属性的尾空格保留，已用分号明确夹具值边界，不修改引擎的原值保留行为。
- 完整路由 `npm run format:check && npm run lint && npm run typecheck:strict && npm run build && npm test` 全部通过：150 文件 / 2030 用例。包括既有颜色识别、主题配对、真实样式表契约及模块依赖无环检查；`git diff --check` 通过。
- 首次 pre-commit 在 Rust 覆盖率编译阶段被系统选中的 Xcode 许可状态拦截，未运行到 SonarQube、未生成提交。已验证本机独立 Command Line Tools 的编译/链接可用；后续提交与推送仅对调用进程设置 `DEVELOPER_DIR=/Library/Developer/CommandLineTools` 重新执行完整钩子，不改变系统工具链选择，不代替用户接受许可，也不跳过覆盖率或门禁。
- 文件/最长代码行（`wc -l` + TypeScript AST）：词法模块 42 / 21、颜色模块 170 / 28、共享引擎 646 / 32、引用回归测试 150 / 50、真实契约测试 845 / 66。无超硬上限。共享引擎超过 600 行讨论阈值，复核后保留其取值、依赖与失败/恢复的一致语义；本轮已将有两个真实消费者的不透明区段识别提取到专责叶模块，避免重复解析或让颜色模块承载通用词法依赖。新测试单独覆盖引用边界，未继续扩大既有 947 行语义测试。既有 66 行测试分组保持并列案例，新增单元均低于 60 行；圈复杂度为 `N/A — no configured complexity tool`，未新增深层控制流。
- 文档路由无配置自动行为检查，已结构化核对设计 §2.1、测试头注、本文矩阵及新模块职责；数据模型不变。未改 Rust 源码，Rust 路由不适用；提交/推送钩子仍分别刷新前端/Rust 覆盖率并执行 SonarQube，最终门禁结果及提交链接随原线程回复记录。
- 构建及门禁临时输出结束后清理并恢复任务前已有产物；无关未跟踪文档 `2026-09-22-quality-assessment.md` 保持不变。

## 原线程处置依据

[字符串中的伪变量依赖](https://github.com/hailingu/PlotWeave/pull/288#discussion_r4073516530)

1. **处置** — 已修复，真实 P2 误报；它提供了上一轮字面色修复之外的新触发条件。
2. **理由与变更** — 提取 `maskCssOpaque` 到 `cssValueSyntax.ts`，用等长空格屏蔽字符串/URL 供 `allVarRefs`、变量定位、悬空检查及简易值链共用；原始切片组装保留字面内容。`colorTokenOf` 同步忽略不透明内容里的括号，避免 fallback 被提前截断。真实依赖（包括未选择 fallback 中的真实引用）仍参与环检测；颜色识别重用该模块。
3. **验证** — 原反例及相邻入口先红后绿；聚焦 188 项、完整 2030 项通过，命令及逐状态映射见上。覆盖真实循环/悬空不被掩盖、根/局部值与字面 fallback 原样保留、字符串误用作颜色仍拒绝。
4. **后续** — 本条无剩余工作。完整 CSS 值文法/选择器层叠仍为既有模型边界，未运行 WebView 像素测试。
